import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: [
    "points-devoted-incorporated-whose.trycloudflare.com",
  ],
};

export default nextConfig;
