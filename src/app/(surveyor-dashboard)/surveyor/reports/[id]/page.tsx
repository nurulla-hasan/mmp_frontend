import { DashboardDynamicPage } from "@/components/shared/dynamic-page";
export default async function Page({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <DashboardDynamicPage label="Survey Report" value={id} description="Report content, status, and PDF generation placeholder will appear here." />; }
